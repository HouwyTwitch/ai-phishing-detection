# АнтиФиш — ML-защита от фишинговых атак

[![Версия](https://img.shields.io/badge/version-1.1.0-blue)](CHANGELOG.md)
[![Точность](https://img.shields.io/badge/accuracy-97%25-brightgreen)](#технологии)
[![Лицензия](https://img.shields.io/badge/license-Proprietary-red)](#лицензия)

**АнтиФиш** — браузерное расширение для защиты от фишинга, использующее ML-модель с точностью **97%** для обнаружения вредоносных сайтов в реальном времени.

> **Автор:** Назаров Андрей Алексеевич, магистрант СГТУ им. Гагарина Ю.А., направление «Информационная безопасность»

---

## Содержание

- [Возможности](#возможности)
- [Архитектура](#архитектура)
- [Быстрый старт](#быстрый-старт)
- [Конфигурация](#конфигурация)
- [Панель администратора](#панель-администратора)
- [API-справочник](#api-справочник)
- [Расширение](#расширение)
- [Docker](#docker)
- [Технологии](#технологии)
- [Лицензия](#лицензия)

---

## Возможности

| Функция | Бесплатно | Премиум |
|---|:---:|:---:|
| Быстрая проверка по базе фишинговых доменов | ✅ | ✅ |
| AI-анализ URL (97% точность) | ✅ | ✅ |
| AI-анализ содержимого страниц | — | ✅ |
| Уведомления о блокировке | ✅ | ✅ |
| Настраиваемая чувствительность | ✅ | ✅ |
| Тёмная тема | ✅ | ✅ |

- **3-этапная проверка:** быстрый список → AI по URL → AI по содержимому
- **Поддержка браузеров:** Chrome, Яндекс.Браузер, Edge (и любые Chromium-браузеры)
- **Офлайн-режим:** основная база доменов хранится локально
- **Автообновление базы** при каждом запуске сервера
- **Соответствие 152-ФЗ** — данные не покидают локальный сервер (при self-hosted развёртывании)

---

## Архитектура

```
ai-phishing-detection/
├── extension/          # Браузерное расширение (Manifest V3)
│   ├── background.js   # Сервис-воркер: логика проверки и кэш
│   ├── content.js      # Контент-скрипт: перехват ссылок
│   ├── blocked.html    # Страница блокировки
│   ├── popup/          # Попап расширения
│   └── settings/       # Страница настроек (options_ui)
│
├── server/             # Flask API-сервер
│   ├── app.py          # Основной API + панель администратора
│   ├── config.py       # Конфигурация через env-переменные
│   ├── Dockerfile      # Docker-образ сервера
│   ├── requirements.txt
│   ├── assets/         # ML-модель, базы доменов, ключи
│   │   ├── api_keys.json      # API-ключи (создаётся автоматически)
│   │   └── license_keys.json  # Лицензионные ключи
│   ├── templates/
│   │   └── admin.html  # Веб-панель администратора
│   └── src/
│       ├── ai/url.py   # ML-детектор (33 признака)
│       ├── black_list.py
│       ├── white_list.py
│       ├── keys.py     # Менеджер API и лицензионных ключей
│       └── utils.py
│
└── docker-compose.yml
```

### Поток проверки URL

```
Браузер открывает URL
       │
       ▼
[FAST] Белый/чёрный список?  ──БЕЛЫЙ──► ✅ Разрешить
       │                     ──ЧЁРНЫЙ──► 🚫 Блокировать
       │ не найден
       ▼
[AI]   ML-анализ URL (33 признака)
       │  phishing > threshold? ─► 🚫 Блокировать
       │  нет угрозы?           ─► ✅ Разрешить
       │  неопределённо
       ▼
[PREMIUM] AI-анализ содержимого страницы (через 60 сек)
       │  фишинг?  ─► 🚫 Блокировать с уведомлением
       │  нет?     ─► ✅ Разрешить
```

---

## Быстрый старт

### 1. Запуск сервера

```bash
cd server

# Создать виртуальное окружение
python -m venv .venv
source .venv/bin/activate   # Windows: .venv\Scripts\activate

# Установить зависимости
pip install -r requirements.txt

# Настроить переменные окружения
cp .env.example .env
# Отредактируйте .env под своё окружение

# Запустить сервер
python app.py
# Сервер запустится на http://0.0.0.0:8787
```

### 2. Установка расширения

1. Откройте `chrome://extensions/`
2. Включите **«Режим разработчика»** (правый верхний угол)
3. Нажмите **«Загрузить распакованное»**
4. Выберите папку `extension/`
5. Иконка АнтиФиш появится в панели инструментов

### 3. Первоначальная настройка

1. Кликните на иконку расширения
2. Нажмите кнопку **⚙ (настройки)**
3. Убедитесь, что **URL API-сервера** соответствует адресу запущенного сервера
4. Нажмите **«Проверить»** для проверки подключения
5. Сохраните настройки

---

## Конфигурация

Все параметры сервера настраиваются через переменные окружения (файл `server/.env`):

| Переменная | По умолчанию | Описание |
|---|---|---|
| `ANTIPHISH_HOST` | `0.0.0.0` | Адрес сервера |
| `ANTIPHISH_PORT` | `8787` | Порт |
| `ANTIPHISH_DEBUG` | `false` | Режим отладки (только для разработки) |
| `ANTIPHISH_API_KEY` | *(пусто)* | Начальный admin-ключ (мигрирует в `api_keys.json`) |
| `ANTIPHISH_THRESHOLD` | `0.65` | Порог вероятности фишинга |
| `ANTIPHISH_CACHE_TTL` | `600` | Время кэширования (сек) |
| `ANTIPHISH_RATE_LIMIT` | `120` | Запросов в минуту с одного IP |
| `ANTIPHISH_LOG_LEVEL` | `INFO` | Уровень логирования |

### Первоначальный admin-ключ

```bash
# Сгенерировать ключ
python -c "import secrets; print(secrets.token_hex(32))"

# Добавить в server/.env
ANTIPHISH_API_KEY=сгенерированный_ключ
```

При первом запуске ключ автоматически сохраняется в `server/assets/api_keys.json` как admin-ключ.  
Дальнейшее управление ключами — через панель администратора.

---

## Панель администратора

Веб-интерфейс доступен по адресу **`http://localhost:8787/admin`**.

### Возможности

| Раздел | Описание |
|---|---|
| **Dashboard** | Статус сервера, размер баз, кэш, ML-модель; очистка кэша |
| **Blacklist** | Просмотр, поиск, добавление и удаление фишинговых доменов |
| **Whitelist** | Просмотр, поиск, добавление и удаление доверенных доменов |
| **URL Tester** | Ручная проверка URL через быстрый или AI-анализ |
| **API Keys** | Управление ключами с ролями `admin` и `user` *(только admin)* |
| **License Keys** | Генерация и отзыв лицензионных ключей `APF-XXXX-XXXX-XXXX` *(только admin)* |

### Роли API-ключей

| Роль | Доступные эндпоинты |
|---|---|
| `admin` | Все эндпоинты, включая управление ключами и списками |
| `user` | `/health`, `/api/v1/fast`, `/api/v1/ai`, `/api/v1/ai-content`, `/api/v1/license/verify` |

### Лицензионные ключи

Лицензионный ключ активирует Премиум-план в расширении. Формат: `APF-XXXX-XXXX`.

- Ключи генерируются в панели администратора (раздел **License Keys**)
- Поддерживаются планы: `premium`, `free`
- Можно задать дату истечения или сделать ключ бессрочным
- Проверка ключа расширением: `POST /api/v1/license/verify`

---

## API-справочник

**Базовый URL:** `http://localhost:8787`

### `GET /health`

Проверка работоспособности сервера.

```json
{
  "status": "ok",
  "version": "1.1.0",
  "model_loaded": true,
  "blacklist_size": 850000,
  "whitelist_size": 1097
}
```

### `POST /api/v1/fast`

Быстрая проверка по белому/чёрному спискам.

```json
{ "link": "https://example.com" }
```

**Ответ:** `{"phishing": false, "source": "whitelist"}`

### `POST /api/v1/ai`

ML-анализ URL без загрузки страницы.

```json
{ "link": "https://example.com", "threshold": 0.65 }
```

**Ответ:** `{"phishing": false, "source": "ai_url", "chance": 0.0812}`

### `POST /api/v1/ai-content` *(Премиум)*

Глубокий анализ с учётом содержимого страницы.

```json
{ "link": "https://example.com", "content": "<html>...</html>", "threshold": 0.65 }
```

### `GET/POST/DELETE /api/v1/blacklist` и `/api/v1/whitelist`

Управление списками *(требуется роль `admin`)*.

```bash
# Добавить домен
curl -X POST http://localhost:8787/api/v1/blacklist \
  -H "Content-Type: application/json" \
  -H "X-API-Key: ваш_ключ" \
  -d '{"link": "phishing-example.com"}'

# Удалить домен
curl -X DELETE http://localhost:8787/api/v1/blacklist \
  -H "Content-Type: application/json" \
  -H "X-API-Key: ваш_ключ" \
  -d '{"link": "phishing-example.com"}'
```

### `GET/POST/DELETE /api/v1/keys` *(admin)*

Управление API-ключами.

```bash
# Создать user-ключ
curl -X POST http://localhost:8787/api/v1/keys \
  -H "X-API-Key: admin_ключ" \
  -H "Content-Type: application/json" \
  -d '{"role": "user", "name": "Пользователь 1"}'

# Список всех ключей
curl http://localhost:8787/api/v1/keys -H "X-API-Key: admin_ключ"
```

### `GET/POST/DELETE /api/v1/license` *(admin)*

Управление лицензионными ключами.

```bash
# Создать лицензию
curl -X POST http://localhost:8787/api/v1/license \
  -H "X-API-Key: admin_ключ" \
  -H "Content-Type: application/json" \
  -d '{"plan": "premium", "expires": "2027-01-01", "note": "Клиент Иванов"}'
```

### `POST /api/v1/license/verify` *(user/admin)*

Проверка лицензионного ключа (используется расширением).

```bash
curl -X POST http://localhost:8787/api/v1/license/verify \
  -H "X-API-Key: user_ключ" \
  -H "Content-Type: application/json" \
  -d '{"key": "APF-ABCD-EF01-2345"}'
# → {"valid": true, "plan": "premium", "expires": "2027-01-01"}
```

### `POST /api/v1/cache/clear` *(admin)*

Очистка кэша URL-проверок.

---

## Расширение

### Настройки (⚙ в попапе → options_ui)

| Параметр | Описание |
|---|---|
| URL API-сервера | Адрес вашего сервера АнтиФиш |
| API-ключ | Совпадает с `ANTIPHISH_API_KEY` на сервере |
| Чувствительность | Порог обнаружения (50–90%) |
| Режим проверки | Авто (при каждом переходе) или ручной |
| Уведомления | Системные уведомления браузера |
| Тёмная тема | Тёмный интерфейс |
| Лицензионный ключ | Активация Премиум-плана (`ANTIPHISH-XXXX-XXXX-XXXX`) |

---

## Docker

```bash
# Подготовить конфигурацию
cp server/.env.example server/.env
# Отредактируйте server/.env

# Запустить
docker compose up -d

# Проверить
curl http://localhost:8787/health

# Логи
docker compose logs -f antiphish-api
```

---

## Технологии

### ML-модель
- Алгоритм: **RandomForestClassifier** (scikit-learn) / XGBoost
- Признаков: **33** (URL-структура, WHOIS, содержимое страницы)
- Точность: **97%** на тестовой выборке
- Сравнение: лучший традиционный аналог — **83%** (по исследованию 145 сервисов)

### Бэкенд
- Flask + Waitress (production WSGI)
- TTL-кэш (потокобезопасный, без доп. зависимостей)
- Rate limiting по IP
- CORS с настраиваемыми источниками
- Ротирующие лог-файлы

### Расширение
- Chrome Extension **Manifest V3**
- Service Worker (background.js)
- `chrome.storage.sync` — настройки
- `chrome.storage.local` — статистика

---

## Лицензия

© 2025 Назаров Андрей Алексеевич. Все права защищены.

Программное обеспечение распространяется на проприетарной лицензии.
Свободное использование, копирование, модификация и распространение запрещены без письменного разрешения правообладателя.

---

*СГТУ им. Гагарина Ю.А. · Кафедра информационной безопасности · Магистратура 2024–2026*
